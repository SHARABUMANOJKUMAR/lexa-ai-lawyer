project_id = "ratlfzpryvunqzqmbuvk"

[functions.legal-chat]
# Auth is enforced in code; disable platform JWT so /health can be polled unauthenticated
verify_jwt = false

[functions.analyze-evidence]
# JWT verification enabled by default (secure)

[functions.generate-fir]
# JWT verification enabled by default (secure)

[functions.sync-to-sheets]
verify_jwt = true
